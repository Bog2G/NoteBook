<h1 style="margin-left: 40%"><%= current_user.email.capitalize.slice(0, current_user.email.index('@')) %> Notes  </h1>


<div id="notes">
  <% @notes.each do |note| %>
    <%= render note %>
  <% end %>
</div>






